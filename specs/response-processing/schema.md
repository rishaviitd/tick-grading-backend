# response-schema

dictionary={
"ANS-1":"cloundinary-url"
}

#student-schema

student_name:"value"

Note:
now the student-schema will also have the response-schema for each student which is basically a dictionary where key is the answer-identifer and value is the url of
the cropped student response for that particular question only.
